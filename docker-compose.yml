services:
  # 1. Vector Database (PostgreSQL + pgvector + pg_search)
  vdb:
    build:
      context: ./pg-ext
      dockerfile: Dockerfile
    container_name: rag_vdb
    restart: always
    env_file:
      - ./settings/.env.poc
    environment:
      - POSTGRES_USER=${PG1_USERNAME:-postgres}
      - POSTGRES_PASSWORD=${PG1_PASSWORD:-password}
      - POSTGRES_DB=${PG1_DATABASE:-vector}
    ports:
      - "5435:5432" # 로컬 포트 충돌 방지를 위해 외부 포트 5435 사용
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${PG1_USERNAME:-postgres} -d ${PG1_DATABASE:-vector}"]
      interval: 5s
      timeout: 5s
      retries: 5

  # 2. Message Broker (RabbitMQ)
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rag_rabbitmq
    restart: always
    env_file:
      - ./settings/.env.poc
    environment:
      - RABBITMQ_DEFAULT_USER=${RABBITMQ_USER:-guest}
      - RABBITMQ_DEFAULT_PASS=${RABBITMQ_PASSWORD:-guest}
    ports:
      - "5672:5672"
      - "15672:15672" # 관리자 UI
    healthcheck:
      test: rabbitmq-diagnostics -q ping
      interval: 5s
      timeout: 5s
      retries: 5

  # 3. Semantic Cache (Redis with Search Module)
  redis:
    image: redis/redis-stack-server:latest
    container_name: rag_redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  # 4. API Server (FastAPI)
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rag_api
    restart: always
    env_file:
      - ./settings/.env.poc
    environment:
      - PG1_HOST=vdb
      - PG1_PORT=5432
      - RABBITMQ_HOST=rabbitmq
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    ports:
      - "8000:8000"
    volumes:
      - .:/app  # [핵심 수정] 로컬 디렉토리를 컨테이너의 /app에 실시간 동기화
      - /app/.venv  # [핵심] 컨테이너 내부의 Linux용 .venv를 보호하는 익명 볼륨
    depends_on:
      vdb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_started
    # 개발 편의성을 위해 FastAPI 자동 재시작(--reload) 옵션 추가
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

  # 5. Background Worker (FastStream)
  worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rag_worker
    restart: always
    env_file:
      - ./settings/.env.poc
    environment:
      - PG1_HOST=vdb
      - PG1_PORT=5432
      - RABBITMQ_HOST=rabbitmq
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - .:/app  # [핵심 수정] 워커도 로컬 코드 실시간 동기화 적용
      - /app/.venv  # [핵심] 동일하게 보호
    depends_on:
      vdb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_started
    command: faststream run src.rag.bulk_source.worker:app

volumes:
  pg_data:
  redis_data: